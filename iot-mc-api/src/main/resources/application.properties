spring.application.name=API para Microcontroladores IoT

# Configurações do PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/API_IOT
spring.datasource.username=nicolas
spring.datasource.password=nicolas190308

# Configurações do JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configuração Token JWT
api.security.token.secret=${JWT_SECRET:secret-key}